stow:
  - alacritty:
      - src: alacritty.yml
        links:
          - "${HOME}/.config/alacritty/alacritty.yml"

  # NEED TO FIX
  - fish:
      - src: bashrc
        links:
          - "${HOME}/.bashrc"

      - src: bash_profile
        links:
          - "${HOME}/.bash_profile"

  - git:
      - src: gitconfig
        links:
          - "${HOME}/.gitconfig"

      - src: gitconfig
        links:
          - "${HOME}/.config/git/gitconfig"

  # NEED TO FIX
  - nvim:
      - src: bashrc
        links:
          - "${HOME}/.bashrc"

      - src: bash_profile
        links:
          - "${HOME}/.bash_profile"

  - starship:
      - src: starship.toml
        links:
          - "${HOME}/.config/starship/starship.toml"

  # NEED TO FIX
  - tmux:
      - src: bashrc
        links:
          - "${HOME}/.bashrc"

      - src: bash_profile
        links:
          - "${HOME}/.bash_profile"

